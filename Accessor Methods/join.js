let array = ['a', 'b', 'c', 'd', 'e'];
let join = array.join('.');
console.log(join);
console.warn(array);

// joins all array elements into a string,with an optional separator.

// alternate answer-
// Create string from array elements.